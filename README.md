
## Docker version 28.2.2
```bash
docker compose up --build
```

Сервер будет доступен на **http://localhost:8080**

По умолчанию в `.envExample` буде

## Для работы переименовать этот файл `.envExample` ->`.env`
